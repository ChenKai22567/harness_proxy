# Harness 组件管理与性能优化交付方案

日期：2026-09-04
状态：本轮功能已经落地；后续项目按优先级保留在第 5 节。

## 1. 本轮问题结论

| 现象 | 根因 | 本轮处理 |
|---|---|---|
| 软件启动的 Codex 出现 `/backend-api/codex/responses` 404、超时及反复重连，而旧脚本正常 | 启动器曾额外注入 Codex 未要求的 `WS_PROXY`、`WSS_PROXY`、`NODE_USE_ENV_PROXY` 与 `ELECTRON_GET_USE_PROXY`；更新后的 Codex 在这组环境下整条 HTTPS/WebSocket 链路异常 | Codex 启动环境与已验证的旧脚本对齐：只设置大写的 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`、`NO_PROXY`，并清除从父进程继承的冲突代理开关；Antigravity 的兼容扩展保持独立 |
| 必须开启 Clash 全局/系统代理才恢复 | 系统代理绕开或掩盖了错误的进程级代理组合，并非 Codex 必须依赖全局模式 | 新启动的受管 Codex 使用标准进程级代理；安全 WebSocket 按 Codex 实现复用 HTTPS/HTTP/ALL 代理路由，不要求修改 Windows 系统代理 |
| 打开偏好设置后主窗口按钮失效 | 旧弹窗曾使用 Tk 全局输入抓取；窗口映射异常时，隐藏弹窗仍可持有输入 | 偏好设置保持非模态、无 `grab_set`，窗口引用在关闭时可靠释放；后台结果统一派发到 Tk 主线程 |
| 从托盘菜单恢复时出现逐渐消失的黑块 | 菜单回调、CustomTkinter 画布首帧和 DWM 恢复动画发生竞争，且恢复流程重复切换窗口状态 | 托盘恢复请求合并，在菜单关闭后仅映射一次；映射前后应用原生表面保护，并强制完成一次子窗口重绘后再交给 DWM |
| 构建或替换启动器时 Codex 一起关闭 | 构建脚本使用 `taskkill /T`；Windows 仍把分离启动的 Codex 视为启动器后代，`/T` 会连带结束整棵树 | 删除 `/T`；测试发布明确禁止停止真实进程；停止应用只允许作用于已验证的受管会话 |
| Codex 更新后出现 WinError 2 | Microsoft Store 更新会更换版本化安装目录，缓存路径已失效 | 每次预检验证文件是否仍存在，重新选择最高有效版本；启动瞬间再次更新时自动刷新并重试一次 |
| 组件页最初读取约 25 秒 | `psutil.process_iter(attrs=...)` 在 Windows 上逐个打开约 560 个进程 | 第一阶段改用 Toolhelp 原生快照，只对目标应用树读取命令行、时间和内存 |
| 手动关闭 Codex 后仍显示运行中，且“接管”失败 | Windows 仍可能枚举零线程的已终止进程对象，或留下无根辅助进程；旧判断只看进程名，组件页还把不可读命令行误判为根进程 | 活动根、存活残留和已终止记录分开判断；已终止记录不再计入运行状态，存活残留不阻止启动，并提供独立的确认清理操作 |
| Codex 正在运行时“重启/终止”被禁用 | 原有防误杀策略把全部外部会话永久设为只读，用户必须绕到组件页手动接管 | 保留安全边界但恢复直接操作：按钮始终可用；外部会话点击后先提示影响、验证根 PID 并接管，再仅控制该根进程树；验证失败则不执行 |
| 点击 Codex 重启后启动器随之退出，且未完成重启 | 当启动器由 Codex 内终端启动时，它会成为待结束 Codex 根进程的真实后代；旧终止器无自我保护，在执行到重新派生前先结束了启动器 | 终止器新增受保护 PID 子树；Codex 停止始终保护启动器自身，随后等待旧 Store/Electron 根完全释放，只有确认退出后才派生新实例 |
| 下拉列表闪现后立即消失 | 鼠标按下阶段创建无边框弹窗并同步强制焦点，随后的同次鼠标释放把焦点交还主窗口，60 ms 焦点检查误判为外部点击 | 菜单不再抢夺焦点；由输入框维持键盘导航，所属窗口按真实点击目标判断内外区域，选择、Escape 或真正外部点击才关闭 |
| 下拉列表圆角外仍出现灰色矩形，字体对勾带弯曲 | `Toplevel` 本身始终是矩形，原实现又把它的底色设为边框灰；选中标记依赖字体字形 | Windows 下使用独立透明色键裁掉弹窗四角，仅保留 CTk 圆角框；对勾改为两段独立直线绘制的 10 px 图标，并用透明等宽图标稳定各行对齐 |
| 偏好设置/组件管理顶部仍低于主窗口 | 居中后上移的算法没有考虑窗口高度差，且在高 DPI 下混用了客户区逻辑尺寸与窗口外框物理坐标 | 改为基于主窗口外框顶部固定上移 24 px，并使用 CTk 窗口缩放后的物理尺寸完成居中和屏幕边界约束 |

## 2. 已交付的组件与子进程管理

主窗口的 Antigravity 与 Codex 卡片均增加“组件管理”。组件页按稳定功能聚合易变化的 Electron/Chromium PID：

| 功能角色 | 用户可见用途 | 控制策略 |
|---|---|---|
| 核心主进程 | 窗口、生命周期和服务调度 | 必需；不能单独关闭 |
| Codex API 服务 | 对话、工具调用及 Responses WebSocket | Codex 必需；不能单独关闭 |
| 界面渲染器 | 页面、编辑器和交互绘制 | 必需；由应用自行维护 |
| 浏览器网络服务 | Chromium 请求、下载和代理连接 | 必需；由应用自行维护 |
| GPU 加速 | 图形合成 | 可勾选；下次启动生效，关闭时添加 `--disable-gpu` |
| 本地存储服务 | 缓存、IndexedDB 和会话数据 | 必需；只读展示 |
| 崩溃恢复服务 | 故障信息与异常恢复 | 仅观察 |
| 系统辅助进程 | Node、音频及其他按需服务 | 仅观察 |
| 未分类进程 | 新版本暂未识别的角色 | 仅观察，绝不据此自动终止 |

可配置策略：

- “纳入一键启动”：分别决定批量启动是否包含 Antigravity/Codex。
- “浏览器代理补丁”：控制 Antigravity 的 Node/npx 代理补丁。
- “GPU 加速”：按应用保存，重启后生效。
- Codex 的 API/WebSocket 代理为必要能力，界面中锁定开启；这里代表标准 HTTPS 代理同时承载安全 WebSocket，不代表注入 `WSS_PROXY`。

### 会话归属与停止安全

启动器在 `managed_sessions.json` 中记录根 PID、进程创建时间、可执行文件路径、会话 ID 和代理地址。执行停止前会重新验证：

1. PID 仍存在；
2. 创建时间未变化，防止 PID 被系统复用；
3. 可执行文件路径一致；
4. 进程仍属于对应应用的核心角色。

外部启动或升级前遗留的会话不会被自动结束，但“停止/重启”保持可用。用户点击后会看到明确确认，启动器随后验证并接管当前根进程；组件页也提供“接管当前会话”和“接管并停止”。接管后只结束该根进程及其实际后代，不按进程名全局杀进程；PID、创建时间、路径或角色任一验证失败都会取消操作。

## 3. 已落地的性能与可靠性优化

- 主窗口完成隐藏布局后一次性映射；偏好设置和组件页按需导入。
- 偏好设置和组件管理均以主窗口外框为参照，顶部固定比主窗口高 24 px；横向居中与屏幕边界使用实际 DPI 缩放尺寸计算。
- 全系统常规状态使用 Unicode Toolhelp 快照，完整扫描每 15 秒一次且在托盘隐藏时暂停。
- 同一轮状态刷新复用 PID、父 PID 与线程数快照；用 Toolhelp 自带的线程数直接排除零线程退出记录，避免逐 PID 查询 Windows 状态。
- 状态签名未变化时不重绘 CustomTkinter 卡片。
- UI 派发循环可见时 50 ms、隐藏时 200 ms，减少后台唤醒。
- 固定 3 个后台 worker；同类操作 single-flight，避免重复点击堆积。
- 组件详情只在组件页打开时采样；关闭后没有额外资源采样。
- 配置读写使用 `RLock`、临时文件、`fsync` 和 `os.replace` 原子替换；旧配置深度合并升级并保留未知键。
- 自动路径与手动路径的开关现在真正生效；自动模式不再误用旧的自定义路径。
- `start_minimized` 已接入偏好设置和启动流程；首次恢复后再启动后台监控。
- 运行日志使用中性灰只读区域，UI 最多保留最近 600 行。
- 主窗口、偏好设置和组件管理统一使用灰白系统色；去除蓝色页面/控件背景，主操作改为深灰，语义色只用于小面积运行状态。
- 容器、控件、徽标和复选框圆角分别从 10/8/6/4 收敛为 8/6/5/3，更接近 Windows 原生层级。
- 下拉框统一为有描边的简洁字段，右侧按钮使用固定 1 px、自绘小型细线箭头；展开内容不再依赖原生 `tk.Menu`，改为带圆角边框、选中标记和中性悬停的自绘列表，并支持方向键、Home/End、Enter 与 Escape。列表既不强制抢焦点也不申请全局输入抓取，避免瞬间消失和主窗口按钮失效。
- 构建在临时发布目录成功完成后才替换正式目录，并保留配置、日志和受管会话记录。

## 4. 本轮测量与验收

以下为 2026-09-04 本机定向测量，不用单次结果替代长期基准：

| 指标 | 结果 |
|---|---:|
| 组件树采样 | 24,661.5 ms → 287.4 ms → Codex 76.6–99.7 ms / Antigravity 30.2–34.6 ms |
| 组件采样降幅 | 相对最初慢路径超过 99%，且仍在后台按需执行 |
| 主窗口进程状态快照（5 次） | 29.0–35.7 ms，平均约 31.7 ms，且在后台线程执行 |
| 本地代理端口探测 | 0.6 ms |
| 经 `127.0.0.1:7890` 的 HTTPS 出站 | HTTP 200，566.9 ms |
| 自动化回归 | 26/26 通过 |
| 源码编译检查 | 通过 |

当前正在运行的 Codex 由已验证正常的旧脚本启动，只继承四个标准大写代理变量。本轮将诊断逻辑同步为 Codex 的真实路由规则：`HTTPS_PROXY`、`HTTP_PROXY` 或 `ALL_PROXY` 命中即可同时判定 API 与安全 WebSocket 已代理。开发、测试和打包不会重启或结束当前会话；下次自然重启时，新启动器将使用与旧脚本相同的 Codex 代理环境。

## 5. 后续性能计划

### P0：发布后真实链路验收

- 在用户自然结束当前 Codex 会话后，用新启动器创建受管会话。
- 关闭 Clash 系统代理，仅保留本地混合端口，确认浏览器/API/WebSocket 三层均为“已代理”，并确认 Codex 子进程中不存在 WS/WSS 或 Node/Electron 强制代理开关。
- 连续打开历史对话、创建新对话及执行工具调用 20 次，确认不再出现 Responses 404 或重连循环。

### P1：可观测性与长时间运行

- 磁盘日志按 1 MiB × 5 文件轮换，增加限频后的监控降级提示。
- 建立 20 次冷启动基准，报告中位数和 P95；分别测量 boot、imports complete、first paint、ready。
- 可见窗口运行 10 分钟、托盘隐藏 30 分钟，记录 CPU、工作集、线程、句柄及 GDI 对象。
- 对 10,000 行日志做压力测试，确认 UI 始终只保留 600 行且滚动无明显延迟。

### P2：事件驱动状态与资源详情

- 使用 Windows 进程启动/退出事件驱动状态刷新，保留 30–60 秒低频快照作容错。
- 组件页增加按角色汇总的 CPU 曲线；仅在页面可见时以 1–2 秒间隔采样。
- 对未知角色建立版本化分类样本，分类未验证前保持只读。

### P3：长对话性能边界

- 将“网络重试耗时”和“本地会话渲染/内存压力”分别记录，避免混为同一问题。
- 对短会话与超长会话分别测量历史加载时间、首个可交互时间和 Codex 进程树内存。
- 若长会话在完整代理下仍慢，再针对 Codex 本身的缓存/数据库做只读分析；启动器不自动清理用户会话数据。

## 6. 发布门槛

- 所有自动化测试通过，测试过程不得停止真实启动器或用户应用。
- 包内图标和托盘透明通道正确，缺失资源时也不得退化为纯蓝色块。
- 偏好设置和组件页均无全局输入抓取；主窗口在其打开时仍可操作。
- 三个窗口使用同一灰白色阶且无蓝色铺底；显式圆角不超过 8 px；两处下拉框的字段与展开菜单样式一致。
- 外部会话未经用户确认和接管验证不得停止；PID 复用验证失败时自动取消归属和控制操作。
- Codex 更新后路径可自修复；启动参数包含 Chromium 代理，子进程只继承标准 HTTP/HTTPS/ALL/NO_PROXY 环境且安全 WebSocket 复用 HTTPS 路由。
- 从托盘菜单、托盘图标和第二实例三种入口恢复时均复用同一条完整绘制路径。
